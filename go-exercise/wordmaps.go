package main

import (
	"golang.org/x/tour/wc"
	"strings"
	"fmt"
)

func wordCount(s string) map[string]int {

	wordMap := make(map[string]int)

	fmt.Println(wordMap)

	return 
}

func main(){
	wordMap := make(map[string]int)
	fmt.Println(wordMap)
	wc.Test(WordCount)
}
